RSpec.describe Wor::Push::Notifications::Aws do
  it "has a version number" do
    expect(Wor::Push::Notifications::Aws::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
